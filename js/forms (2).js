const forms = () => {
    const form = document. querySelectorAll('form'),
         input = document. querySelectorAll('input');
};
    const message = {
        loading: 'Загружка...'
        success: ' Спасибо ...'
        faiture: 'Не так ....' 
    }
export default forms;